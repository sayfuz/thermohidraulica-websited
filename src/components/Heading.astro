---
import type { ClassNameProps as Props } from "~/types";

const { class: className } = Astro.props;
const wrapperClasses = `max-w-[100%] prose ${className}`;
---

<div class={wrapperClasses}>
  <Fragment>
    <slot name="heading-title" />
  </Fragment>
  <Fragment>
    <slot name="heading-content" />
  </Fragment>

  <slot />
</div>
